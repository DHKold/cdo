#ifndef CDO_CORE_OUTPUT_H
#define CDO_CORE_OUTPUT_H

// Backward-compatibility wrapper: output is now in commons/ (utility layer).
// core/output.h forwards to commons/output.h so existing includes continue to work.
#include "commons/output.h"

#endif // CDO_CORE_OUTPUT_H
